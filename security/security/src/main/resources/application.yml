eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
server:
  port: 8090
spring:
  application:
    name: users
  config:
    import: optional:configserver:http://localhost:8888
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/jwt_security
    username: postgres
    password: 1651
  jpa:
    hibernate:
      ddl-auto: create-drop
    show_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  cache:
    type: redis
    redis:
      host: 'redis-11797.c274.us-east-1-3.ec2.redns.redis-cloud.com'
      port: 11797
      password: 'QrFTxCnfnHJfXqsm9CAwUHWLJj8puIp5'
      database: 0
      timeout: 60000 # Optional: Timeout in milliseconds for connection (60 seconds)
      jedis:
        pool:
          max-active: 8
          max-wait: -1ms
          max-idle: 8
          min-idle: 0



